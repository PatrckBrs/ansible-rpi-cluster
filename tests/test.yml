---
- hosts: localhost
  remote_user: root
  roles:
    - role_under_test
    
  tasks:
   - name: Installation tools
     apt: name={{item}} state=present update_cache=yes
     with_items:
      - htop
      - vim
      - ufw
      - rkhunter
      - fail2ban
      - mutt
      - logwatch
